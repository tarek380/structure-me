# Vercel build needs scripts/ (build-insights.mjs, patch-studio-paths.mjs)
# and studio/ (Sanity Studio source) — do NOT exclude those.
# Only exclude artifacts that shouldn't be uploaded with the source.
node_modules
studio/node_modules
studio/.sanity
studio/dist
studio-app
.git
.DS_Store
